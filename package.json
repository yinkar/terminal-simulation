{
  "name": "terminal-simulation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "vite build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^4.1.0"
  },
  "dependencies": {
    "p5": "^1.6.0",
    "gh-pages": "^4.0.0"
  }
}
